# 1. create a venv called .venv (any name works)
python3 -m venv .venv

# 2. activate it
source .venv/bin/activate      # zsh/bash
# (you’ll see the prompt change to (.venv))

# 3. install the deps *inside* the venv
pip install -r requirements.txt

# 4. run the bot (python now points to the venv interpreter)
python -m flowbot.bot           # add flags / env vars as needed